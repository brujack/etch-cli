[package]
name = "jsonschemagen"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
comtrya-lib = { path = "../lib" }
schemars = "1.2"
serde_json = "1.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(tarpaulin_include)'] }
