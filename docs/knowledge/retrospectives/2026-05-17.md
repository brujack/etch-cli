# Retrospective — 2026-05-17 (etch-cli)

**Period:** 2026-04-17 to 2026-05-17
**Repo(s):** etch-cli
**PRs merged:** 28 (PRs #1–28)
**Direct master commits:** ~80 (mostly docs: specs, plans, action catalog, status updates)
**Total commits:** 117

## Summary

A foundational + explosive growth period. PR #1 was **the Phase-1 rename and audit** (May 2) — etch-cli officially forked from comtrya. From May 2–17, the repo executed a **systematic coverage push** (39% → 75%+ via PRs #3–18) and then an **action-shipping sprint** (PRs #19–28 added file.chmod, file.copy/chown/link privileged, shellexpand, brew.bundle, package.install cask, mas.install, brew.upgrade/cleanup/mas.upgrade, command.run skip_if_exists — 10 new actions in 14 days). The `etch-cli-new-action` skill (created mid-period) clearly accelerated the action-shipping pace.

## What got easier

- **Action-shipping is templated** — the etch-cli-new-action skill enforces spec → plan → Rust TDD → enum registration → action catalog update → closeout. 10 actions shipped in ~2 weeks.
- **Coverage gate enforced** — 70% on Linux CI (Linux measures ~72%); etch-cli has a documented tdd.md exception for structurally uncoverable code.
- **Provider/platform simplified** — removed Windows cfg stubs, ListContext variant, dead code. Repo pared down to Ubuntu + macOS only (PR #3).
- **Smoke tests established** — `smoke-tests/` directory with five manifests for VM validation.
- **Docs-lint and docs-build CI jobs** (PR #18) keep mdbook docs current.

## Recurring gotchas (action required)

- **Cargo.lock out-of-sync with Cargo.toml** — observed this session (shellexpand was in lib/Cargo.toml but Cargo.lock hadn't been committed). → **Action:** make sure `cargo test` runs before commits that touch Cargo.toml, so Cargo.lock updates land in the same PR.
- **`gh --repo brujack/etch-cli` flag required** for all gh commands — established memory exists for this. Continuing to bite occasionally (transcript analysis missed PRs without the flag).
- **61 touches to `docs/superpowers/README.md`** — extreme even given the velocity. Each action shipping triggers ~3 status changes (Pending → In Progress → Done). The new "do status updates on master after merge" rule should reduce conflicts but doesn't reduce the touch count.

## Skill usage

- **Most invoked:** etch-cli-new-action (10 actions × spec/plan/closeout), pr-review (28 PRs), finishing-a-development-branch (each merge), superpowers:test-driven-development (per action implementation)
- **Updated this period (in ai-config):** etch-cli-new-action skill iterated 4x (BATS gotcha, strict TDD, mutants requirement, brainstorming gate)
- **Notable absence:** `adr` skill not invoked — 4 ADRs were added in docs/adr/ (PR mentions "add docs/adr/ with 4 architectural decision records") but the adr skill didn't exist when they were written. Going forward, use the skill.

## Friction points

- **Cargo.lock sync drift** — see above
- **Action catalog updates require manual table edits in CLAUDE.md** — could be automated from the actions/mod.rs match arms but probably not worth the engineering for a personal tool
- **No `mutants` Makefile target on etch-cli crates** — math has it via cargo-mutants CI workflow; etch-cli does not. Earlier analysis judged etch-cli "less critical" for mutation testing, but with 10+ actions and a stabilizing action API, it may be worth adding.

## Metrics

- **Coverage trend:** 39% → 75%+ (Linux CI), exception documented for hard-to-cover code
- **Actions in catalog:** grew from ~4 to 14+ this period
- **PR cycle time:** Same-day for most; CI gate has 6 jobs (test, cargo-audit, secret-scan, snyk-scan, docs-lint, docs-build)
- **Memory files added this period:** ~5 (homebrew-etch-patterns, etch-manifest-authoring, worktree-docs-conflict, step-initializer-constraints, testing-gotchas refinements)
- **Top-touched files:** `docs/superpowers/README.md` (61), `CLAUDE.md` (23), `.github/workflows/ci.yml` (11)

## Actions for next period

- [ ] Consider adding `cargo-mutants` workflow for etch-cli (math-style) — with 14 actions and growing, mutation testing of the manifest parsing and step execution logic would catch test-shape problems
- [ ] When Cargo.toml changes, ensure `cargo test` runs locally before commit so Cargo.lock syncs land in the same commit (codify in etch-cli-new-action skill?)
- [ ] If action catalog churn continues, consider scripting `CLAUDE.md` action-catalog generation from `lib/src/actions/mod.rs`
- [ ] Run `cargo audit` locally once to baseline (CI job now exists); track any advisories found

## Calibration note

10 new actions in 2 weeks with consistent quality (each with spec, plan, tests, docs update, action catalog entry) is strong output. The etch-cli-new-action skill is the load-bearing piece that made this possible. The repo is now past "fork rename + audit" and squarely in feature-development mode.
