actions:
    # Download a file and set ownership in a single action.
    # owned_by_user / owned_by_group applied after download.
    - action: file.download
      from: https://example.com/config.yaml
      to: /tmp/config.yaml
      owned_by_user: nobody
      owned_by_group: nobody
