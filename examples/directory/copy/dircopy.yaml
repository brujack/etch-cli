actions:
    # Copy a directory recursively. `from` is relative to the manifest directory.
    # `to` is an absolute destination path (tilde expanded).
    - action: directory.copy
      from: mydir
      to: /tmp/dircopy
