# Can define variables in the etch.yaml
# See etch.yaml in the examples directory for an example
# Or define variables in the command line 

actions:
  - action: command.run
    command: echo
    args:
      - "{{ variables.hello }}"
  - action: command.run
    command: echo
    args:
      - "{{ variables.foo }}"
